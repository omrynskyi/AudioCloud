/**
 * Phase 0 placeholder shell.
 *
 * The real layout arrives in Phase 9 (canvas centre, inspector right, library and
 * filters left). Until then this exists only to prove the window renders.
 */
export default function App() {
  return (
    <main className="flex h-full w-full items-center justify-center">
      <p className="text-sm tracking-widest text-neutral-500 uppercase">AudioBank</p>
    </main>
  );
}
